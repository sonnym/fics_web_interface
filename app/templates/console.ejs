<tab heading="console" ng-controller="ConsoleCtrl" active="tabs.console.active" select="">
  <pre class="console" scroll-glue>
    <div ng-repeat="line in output track by $index">{{ line }}</div>
  </pre>
</tab>
